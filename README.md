R-Examples
==========

Small R scripts

mergeTest.R -- demonstrates huge performance differences when merging data frames by "row.names" or by
a column of strings storing "Name". Simply source('mergeTest.R') and run slowMerging() to see demonstration

